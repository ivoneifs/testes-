# Segredos — NUNCA versionar
.env
.env.*
!.env.example

# Ambiente Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Diagnóstico do auto-teste (recriável)
data/catalog_diagnostic.json

# Planilha de origem: fica fora; o banco versionado já é suficiente para rodar.
# (para regerar o banco: coloque a planilha em data/ e rode python -m server.build_db)
*.xlsx
data/*.xlsx

# NOTA: data/neuro_normas.db É versionado para o deploy (Coolify) funcionar sem passos manuais.

# Sistema / editores
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
